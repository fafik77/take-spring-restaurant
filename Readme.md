# Restaurant management system

Starting this API:
1. copy `.env_template` to `.env`
2. in IntelliJ IDEA make sure to add the `.env` file or its content beffore running the app
3. (on Windows) run `run compose up.bat`
4. make sure the postgres is up end running
